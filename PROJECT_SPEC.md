# 프로젝트 명세서 — 트레이딩 학습 사이트 (코드명: **호가창 / Hogachang**)

---

## 1. 한 줄 정의

> 아무것도 모르는 상태에서 시작해, "돈이란 무엇인가"부터 순서대로 쌓아 올려
> **스스로 판단하는 트레이더**가 되기까지의 학습 경로를 관리하는 개인용 웹앱.

**작성자:** 1인 (본인). 회원가입·결제 없음.
**공개 범위:** **인터넷 전체 공개.** 커스텀 도메인으로 배포한다. 방문자는 레슨·용어사전·계산기·공개된 매매일지를 읽을 수 있다. 쓰기 권한은 본인 브라우저에만 있다.
**핵심 가치:** 순서를 강제한다 · 진도를 잊지 않게 한다 · 배운 걸 손으로 굴리게 한다.

---

## 2. 이 사이트가 하는 일 / 안 하는 일

**한다**
- Level 0~10 커리큘럼을 순서대로 잠금 해제하며 진행
- 각 레슨: 본문 → 퀴즈 → 실습 과제 → 자기 요약 → 완료 체크
- **매일 데일리 테스트 8문항** (어제 배운 것 + 복습 도래 + 과거 무작위)
- **레벨 졸업시험** — 통과해야 다음 레벨 잠금 해제
- 진도·연속학습일·정답률 추이 추적
- 틀린 문제 자동 오답노트 + 간격반복 복습
- 용어를 본문에서 클릭하면 뜻이 뜨는 용어사전
- 계산기 도구 모음 (복리, 포지션 사이징, 손익비, MDD 등)
- 학습 노트 (레슨별 내 메모)

**안 한다**
- 실시간 시세 제공, 종목 추천, 자동매매
- 수익률 예측, 투자 권유
- 다른 사람과의 랭킹 비교 (혼자 하는 공부다)
- **매매일지 — 현재 범위 밖.** Level 6 도달 시점에 추가한다 (13장 참고)

---

## 3. 정보 구조 & 라우팅

```
/                          대시보드 (진도 캔들차트, 오늘 할 일, 연속일)
/curriculum                전체 커리큘럼 트리 (Level 0~10)
/learn/[level]             레벨 개요 + 모듈 목록 + 목표/체크포인트
/learn/[level]/[lesson]    레슨 본문 (핵심 / 비유 / 사례 / 흔한 오해 / 퀴즈 / 실습)
/daily                     오늘의 데일리 테스트 (8문항)
/exam/[level]              레벨 졸업시험 (25문항)
/review                    오답노트 + 오늘 복습할 카드 (SRS)
/glossary                  용어사전 (검색 + 레벨 필터)
/tools                     계산기 허브
/tools/compound            복리 계산기
/tools/position-sizing     포지션 사이징 계산기
/tools/risk-reward         손익비 / 기대값 계산기
/tools/drawdown            낙폭 회복 시뮬레이터
/tools/monte-carlo         몬테카를로 자본곡선 시뮬레이터
/tools/leverage-etf        레버리지 ETF 음의 복리 시뮬레이터
/notes                     내 학습 노트 모아보기
/principles                나의 투자원칙서 (IPS) — Level 9에서 잠금 해제
/stats                     학습 통계 (레벨별 소요시간, 퀴즈 정답률 추이)
```

---

## 4. 콘텐츠 구조

### 4.1 파일 배치
```
content/
  levels/
    00-money/
      _level.mdx                 # 레벨 개요·목표·체크포인트
      01-01-barter-to-money.mdx
      01-02-three-functions.mdx
      ...
    01-flow-of-money/
    02-macro-indicators/
    03-market-structure/
    04-fundamental-analysis/
    05-technical-analysis/
    06-strategy/
    07-risk-management/
    08-psychology/
    09-going-live/
    10-advanced/
  glossary.json
```

### 4.2 레슨 MDX 프론트매터 (고정 스키마)
```yaml
---
id: "0-1-1"
level: 0
module: "0-1"
title: "물물교환에서 화폐까지"
subtitle: "인류는 왜 돈을 발명했나"
estimatedMinutes: 15
difficulty: 1            # 1~5
prerequisites: []        # 선행 레슨 id 배열
tags: ["화폐사", "기초"]
glossary: ["법정화폐", "상품화폐"]   # 이 레슨에서 처음 나오는 용어
quiz:
  - q: "돈의 3가지 기능이 아닌 것은?"
    choices: ["교환의 매개", "가치의 척도", "가치의 저장", "수익의 보장"]
    answer: 3
    explain: "돈은 수익을 보장하지 않는다. 오히려 인플레이션 때문에 시간이 갈수록 구매력을 잃는다."
practice:
  - "내 지난달 지출을 생존/소비/투자/낭비 4가지로 분류해보기"
checkpoint: "물물교환의 '욕망의 이중일치' 문제를 예시를 들어 설명할 수 있다"
---
```

### 4.3 레슨 본문 고정 섹션
모든 레슨은 이 6개 섹션 순서를 지킨다. Claude Code는 이 골격을 벗어나지 않는다.

1. `## 한 줄로` — 이 레슨의 결론 한 문장
2. `## 왜 알아야 하나` — 트레이딩과의 연결고리 (여기가 빠지면 안 됨)
3. `## 개념` — 본문
4. `## 비유로 이해하기` — 일상 사례로 치환
5. `## 실제 시장에서는` — 구체 사건/숫자 (연도, 지표, 사례명 명시)
6. `## 흔한 오해` — 초보가 여기서 틀린다

그 뒤 퀴즈·실습은 프론트매터에서 자동 렌더링.

### 4.4 커스텀 MDX 컴포넌트
| 컴포넌트 | 용도 |
|---|---|
| `<Term>기준금리</Term>` | 용어사전 툴팁 |
| `<Callout type="warn\|tip\|money">` | 강조 박스 |
| `<Formula>` | 공식 (KaTeX) |
| `<Compare a b>` | 좌우 비교 카드 |
| `<Timeline>` | 사건 연표 |
| `<MiniChart>` | 개념 설명용 정적 SVG 차트 |
| `<TryIt tool="position-sizing">` | 본문에서 계산기 바로 실행 |
| `<Checkpoint>` | 레슨 말미 자가진단 |

---

## 5. 기능 명세

### 5.1 진도 시스템
- 레슨 상태: `locked` / `available` / `in_progress` / `completed` / `needs_review`
- 잠금 해제 조건: 선행 레슨 완료 + **퀴즈 정답률 80% 이상** + **자기 요약 3줄 이상 작성**
- **자기 요약(`MySummary`)**: 모든 레슨 하단 필수 입력란. "이 레슨을 처음 듣는 사람에게 설명한다면?" 프롬프트를 띄운다. 이게 없으면 완료 처리 불가. 읽기만 하는 학습을 구조적으로 막는 장치다.
- 레벨 완료 조건: 해당 레벨 전 레슨 완료 + 레벨 체크포인트 자가 체크
- **되돌아가기는 항상 자유** (복습에 잠금 없음)

### 5.2 퀴즈
- 객관식 + O/X + 계산형(숫자 입력)
- 오답 즉시 해설 노출
- 오답은 자동으로 `/review` 큐에 들어감
- 재응시 가능, 시도 횟수 기록

### 5.3 데일리 테스트 ★ 매일의 루틴

**목적:** 읽은 것을 꺼내게 만든다. 진도가 나가도 앞부분이 살아있게 유지한다.

- 하루 1회, 8문항, 목표 소요 3~5분
- 대시보드 최상단에 "오늘의 테스트" 카드. 안 풀면 계속 남아있다
- **출제 구성 (고정 비율)**

| 출처 | 문항 | 목적 |
|---|---|---|
| 직전 학습 레슨 | 3 | 단기 정착 |
| SRS 복습 도래 카드 | 3 | 망각 곡선 대응 |
| 전체 이력 무작위 | 2 | 장기 유지 확인 |

- 문항이 부족하면(학습 초기) 직전 레슨 비중을 늘려 채운다. 최소 4문항이면 성립
- **해설은 8문항을 모두 제출한 뒤 한 번에 공개.** 중간 공개 금지 — 시험의 의미가 사라진다
- 오답은 SRS 큐로 자동 편입, `lapses` 증가
- **오답률이 높은 문항에 출제 가중치를 부여한다** (약한 곳이 더 자주 나온다)
- 결과 저장: `dailyTests[date] = { score, total, questionIds, wrongIds }`
- **연속학습일(streak)은 레슨 진도가 아니라 데일리 테스트 완료 기준으로 센다**

### 5.4 레벨 졸업시험

- 레벨의 전 레슨 완료 시 응시 가능
- 25문항, 시간 제한 없음, 해설은 제출 후 일괄 공개
- **합격선 80%.** 불합격 시 24시간 뒤 재응시, 문항은 새로 뽑는다
- 불합격 시 틀린 문항의 소속 레슨을 `needs_review`로 표시하고 "여기부터 다시 보세요" 안내
- 합격하면 다음 레벨 잠금 해제 + 레벨 배지(`--brass` 색상, 유일한 금색 사용처)

### 5.5 문제은행

- 별도 파일을 만들지 않는다. **모든 레슨 MDX의 `quiz` 프론트매터를 빌드 타임에 수집**해 문제은행을 구성한다
- 각 문항에 자동 부여되는 메타: `lessonId`, `level`, `module`, `difficulty`, `tags`
- 레벨 졸업시험용으로 프론트매터에 `examOnly: true` 문항을 추가로 넣을 수 있다 (레슨 퀴즈에는 안 나오고 시험에만 출제)
- 문항 최소 수량 기준: **레슨당 3문항 이상.** 레슨을 새로 쓸 때 이 기준을 만족해야 한다

### 5.6 복습 (SRS)
- 간격: 1일 → 3일 → 7일 → 16일 → 35일
- 맞히면 다음 간격, 틀리면 1일로 리셋
- 대시보드에 "오늘 복습할 카드 N개" 표시

### 5.7 계산기 (명세)

| 도구 | 입력 | 출력 |
|---|---|---|
| 복리 계산기 | 원금, 월적립, 연수익률, 기간, 물가상승률 | 명목/실질 최종금액, 연도별 그래프 |
| 포지션 사이징 | 총자본, 리스크%, 진입가, 손절가 | 매수 수량, 총 투입금액, 손실 시 금액 |
| 손익비/기대값 | 승률, 평균이익, 평균손실, 거래횟수 | R:R, 기대값, 100거래 시뮬레이션 |
| 낙폭 회복 | 손실률 | 필요 수익률 + 손실률 곡선 |
| 몬테카를로 | 승률, 손익비, 리스크%, 거래수, 시행횟수 | 자본곡선 밴드, MDD 분포, 파산확률 |
| 레버리지 ETF | 기초자산 일간 수익률 시퀀스, 배수 | 기초 vs 레버리지 누적 비교 |

> 모든 계산기는 **결과 옆에 "이게 왜 이렇게 나오나" 설명 한 줄**을 붙인다. 계산만 하면 학습 도구가 아니다.

### 5.8 매매일지 — ⏸ 보류 (13장으로 이동)
필드: 날짜 / 종목 / 방향(롱·숏) / 진입가 / 청산가 / 수량 / 손익 / **적용 전략** / **규칙 준수 여부(O/X)** / 진입 시 감정(1~10) / 청산 시 감정 / 배운 점 / 스크린샷
통계: 규칙 준수 거래 vs 위반 거래의 손익 비교 ← 이 화면이 이 기능의 존재 이유다.

**공개 설정 (필수 구현)**
- 일지 항목마다 `visibility: 'public' | 'private'` 필드. **기본값은 `private`.** 공개는 명시적으로 켜야 한다.
- 금액 표시 모드 전역 토글: `absolute`(원화 금액) / `percent`(% 만) / `R`(리스크 배수만). **공개 페이지 기본값은 `percent`.**
- 감정 점수와 "배운 점"은 공개해도 무방하나, 개별 숨김 가능.
- 전역 킬스위치: 설정에서 `journalPublic: false` 하나로 `/journal` 전체를 비공개 전환.
- 비공개 항목은 서버에서 아예 렌더링하지 않는다. CSS로 숨기는 방식 금지.

### 5.9 대시보드
- 최상단: **오늘의 테스트 카드** (미완료 시 강조, 완료 시 오늘 점수 표시)
- **학습 캔들차트 + 정답률 이동평균선**
- 전체 진도 %, 현재 레벨, 연속 학습일(streak)
- "이어서 하기" 버튼 (마지막 레슨으로 바로 이동)
- 오늘 복습 카드 수
- 최근 작성한 자기 요약 3건

---

## 6. 디자인 방향

### 6.1 컨셉
**"호가창의 밀도 + 회계장부의 규율."**
화려하지 않다. 숫자가 주인공이고, 색은 정보일 때만 쓴다.
한국 증시의 관습(상승=빨강, 하락=파랑)을 그대로 UI 언어로 가져온다. 완료는 빨강, 미완/오답은 파랑.

### 6.2 컬러 토큰
```css
--ink:      #101418;   /* 텍스트, 거의 검정에 청색기 */
--paper:    #E9EBEC;   /* 배경, 차가운 회백 (크림 아님) */
--surface:  #FFFFFF;   /* 카드 */
--line:     #C9CDD1;   /* 헤어라인 */
--muted:    #6E767D;   /* 보조 텍스트 */
--rise:     #C8323C;   /* 상승/완료/정답 */
--fall:     #1F5AA8;   /* 하락/미완/오답 */
--brass:    #9A7B3F;   /* 레벨 완료 훈장. 극도로 절제해서 사용 */
```
다크모드: `--ink`/`--paper` 반전, rise/fall은 채도만 살짝 올림.

### 6.3 타이포그래피
- **디스플레이(제목):** Pretendard ExtraBold, `letter-spacing: -0.035em`, 크게 쓴다
- **본문:** Pretendard 400/500, `line-height: 1.8` (긴 글 읽는 사이트다)
- **데이터·숫자·코드:** IBM Plex Mono, 모든 숫자에 `font-variant-numeric: tabular-nums`
- **인용:** Noto Serif KR — **레벨 개요 페이지의 인용문에만** 사용. 남발 금지.

### 6.4 시그니처 요소 — 학습 캔들차트
대시보드 상단에 내 학습 이력이 **캔들차트**로 그려진다.

- 캔들 1개 = 레슨 1개, x축은 학습한 순서
- **몸통 길이** = 퀴즈 정답률
- **위꼬리** = 실습 과제 완료
- **아래꼬리** = 재시도 횟수
- **색** = 한 번에 통과했으면 빨강(rise), 재시도했으면 파랑(fall)
- 레벨 경계마다 세로 구분선 + 레벨명 라벨
- 호버 시 해당 레슨 정보 툴팁

**그 위에 정답률 이동평균선을 겹쳐 그린다.**
- 데일리 테스트 점수의 5일 이동평균(MA5)을 실선으로
- 20일 이동평균(MA20)을 점선으로
- MA5가 MA20을 하향 돌파하면 "복습 필요" 배지 표시 → `/review`로 유도
- 즉 **내 학습 상태를 골든크로스/데드크로스로 읽는다.** Level 5에서 배울 개념을 내 데이터로 먼저 체감하게 하는 장치다

즉, **공부를 하면 내 차트가 그려진다.** 이 사이트에서 유일하게 눈에 띄는 요소이고, 나머지 UI는 전부 조용하게 간다.

### 6.5 레이아웃
```
데스크탑 (≥1280px)
┌──────────┬────────────────────────────┬──────────┐
│ 레벨 트리 │        레슨 본문            │ 목차     │
│ (고정)   │        (max-w 720px)       │ + 용어   │
│ 240px    │                            │ 240px    │
└──────────┴────────────────────────────┴──────────┘

모바일: 1열. 레벨 트리는 하단 시트, 목차는 상단 접힘.
```

### 6.6 품질 기준 (말없이 지킨다)
모바일 반응형 / 키보드 포커스 링 보임 / `prefers-reduced-motion` 존중 / 본문 대비비 4.5:1 이상 / 애니메이션은 페이지 전환과 캔들차트 등장에만.

---

## 7. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | **Next.js 15 (App Router) + TypeScript** | MDX 콘텐츠 + 라우팅에 최적 |
| 스타일 | **Tailwind CSS v4** | CSS 변수 토큰과 궁합 |
| 콘텐츠 | **MDX** (`next-mdx-remote` 또는 `@next/mdx`) | 본문에 인터랙티브 컴포넌트 삽입 |
| 수식 | KaTeX (`rehype-katex`) | 금융 공식 |
| 차트 | **Recharts** + 커스텀 SVG | 학습 캔들차트는 SVG 직접 구현 |
| 저장소 | **1단계: localStorage** → **2단계: SQLite + Prisma** | 혼자 쓰니 초기엔 로컬로 충분 |
| 상태 | Zustand (진도·설정) | 가볍다 |
| 테스트 | Vitest (계산기 로직 필수) | 금융 계산은 틀리면 안 됨 |
| 배포 | Vercel 또는 로컬 | 개인용 |

> ⚠️ **localStorage 스키마는 반드시 버전 필드를 넣는다.** (`{ version: 1, ... }`)
> 나중에 DB로 옮길 때 마이그레이션 함수가 필요하다.

---

## 8. 폴더 구조

```
hogachang/
├── app/
│   ├── page.tsx                    # 대시보드
│   ├── curriculum/page.tsx
│   ├── learn/[level]/page.tsx
│   ├── learn/[level]/[lesson]/page.tsx
│   ├── review/page.tsx
│   ├── glossary/page.tsx
│   ├── tools/[tool]/page.tsx
│   └── layout.tsx
├── components/
│   ├── mdx/                        # Term, Callout, Formula, TryIt …
│   ├── progress/                   # LearningCandleChart, StreakBadge, LevelTree
│   ├── quiz/
│   ├── tools/                      # 계산기 UI
│   └── ui/
├── lib/
│   ├── content.ts                  # MDX 파싱, 레슨 목록
│   ├── progress.ts                 # 진도 로직, 잠금 해제 규칙
│   ├── srs.ts                      # 간격반복 알고리즘
│   ├── finance/                    # 순수 계산 함수 (테스트 필수)
│   │   ├── compound.ts
│   │   ├── positionSizing.ts
│   │   ├── expectancy.ts
│   │   ├── drawdown.ts
│   │   └── monteCarlo.ts
│   └── storage.ts                  # localStorage 추상화 (나중에 DB로 교체 가능하게)
├── content/                        # 위 4.1 참고
├── types/
└── tests/
```

---

## 9. 개발 마일스톤

### M0 — 뼈대 + 배포 (1일)
Next.js + TS + Tailwind 초기화, 컬러/폰트 토큰 세팅, 기본 레이아웃 3열, 라우트 껍데기.
**GitHub 리포 생성 → Vercel 연결 → 커스텀 도메인 연결까지 M0에서 끝낸다.**
**완료 기준:** 내 도메인에 접속하면 디자인 토큰이 적용된 빈 페이지들이 뜨고 서로 이동된다.

### M1 — 콘텐츠 파이프라인 (2일)
MDX 로더, 프론트매터 파싱, `content.ts`, 레벨/레슨 트리 생성, MDX 컴포넌트 5개(`Term`/`Callout`/`Formula`/`Compare`/`Checkpoint`).
**완료 기준:** 샘플 레슨 3개가 실제로 렌더링된다.

### M2 — 진도 + 퀴즈 + 백업 (3일)
`storage.ts`, `progress.ts`, 잠금 해제 로직, 레슨 퀴즈 컴포넌트, 자기 요약 입력란, 완료 처리, JSON 내보내기/불러오기.
**완료 기준:** 레슨을 풀고 요약을 쓰면 다음 레슨이 열린다. 새로고침해도 유지되고, 백업 파일로 복원된다.

### M3 — 대시보드 + 학습 캔들차트 (2일)
대시보드, streak, "이어서 하기", **학습 캔들차트 SVG 구현**.
**완료 기준:** 진도가 캔들로 그려지고 호버 툴팁이 뜬다.

### M4 — 계산기 6종 (3일)
`lib/finance/*` 순수 함수 + Vitest 테스트 먼저, 그다음 UI. `<TryIt>`으로 본문 삽입.
**완료 기준:** 모든 계산 함수에 테스트가 있고 통과한다.

### M5 — 시험 시스템 (3일)
문제은행 수집(`lib/questionBank.ts`), 출제 알고리즘, `/daily` 데일리 테스트, `/exam/[level]` 졸업시험, SRS, 오답노트, 정답률 이동평균선 연동.
**완료 기준:** 매일 접속하면 8문항이 규칙대로 뽑혀 나오고, 결과가 차트에 반영된다.

### M5.5 — 용어사전 (1일)
glossary 툴팁 연동, 용어 검색, 레벨 필터.

### M6 — 콘텐츠 채우기 (지속)
Level 0부터 순서대로 레슨 작성. **한 번에 한 모듈씩.**

---

## 10. 콘텐츠 작성 원칙 (Claude Code가 레슨을 쓸 때)

1. **한국 독자 기준.** 사례는 한국 시장·한국 경제를 우선하되, 미국 시장도 반드시 함께 다룬다.
2. **숫자를 넣는다.** "많이 올랐다" ❌ / "2020년 3월 코스피는 한 달간 약 30% 하락했다" ⭕
3. **연도와 사건명을 명시한다.** 1971 닉슨쇼크, 2008 리먼, 2020 코로나 폭락, 2022 긴축, 2023 SVB.
4. **투자 권유 문구 절대 금지.** "지금 사라", "유망하다", "추천" ❌. 개념 설명과 사례 분석만.
5. **실시간 시세·현재 주가를 본문에 하드코딩하지 않는다.** 금방 틀린 정보가 된다.
6. **레슨 1개는 15~20분 분량.** 넘으면 쪼갠다.
7. **모든 레슨은 "왜 알아야 하나"에서 트레이딩과 연결한다.** 연결이 안 되면 그 레슨은 뺀다.
8. **어려운 용어는 첫 등장 시 `<Term>`으로 감싸고 glossary.json에 등록한다.**

---

## 11. 배포 & 공개 운영

### 11.1 배포 파이프라인
```
로컬 → GitHub → Vercel (자동 빌드) → 커스텀 도메인
```
- `main` 브랜치 푸시 시 자동 배포
- PR 생성 시 프리뷰 URL 자동 생성 (콘텐츠 검토용으로 유용)
- Vercel 무료(Hobby) 플랜으로 충분. HTTPS 자동.

### 11.2 도메인 연결 절차
1. Vercel 프로젝트 → Settings → Domains → 도메인 입력
2. Vercel이 알려주는 레코드를 도메인 등록업체 DNS에 추가
   - 루트 도메인(`example.com`): **A 레코드** → `76.76.21.21`
   - 서브도메인(`www`): **CNAME** → `cname.vercel-dns.com`
   - ※ 실제 값은 Vercel 화면에 표시되는 것을 따를 것
3. 전파 대기 (보통 10분~1시간, 최대 48시간)
4. `www` → 루트 리다이렉트 설정

### 11.3 공개 사이트이므로 필수인 것
- `app/layout.tsx`에 metadata: title, description, `openGraph`, `twitter`
- `app/opengraph-image.tsx` — OG 이미지 (학습 캔들차트 모티프)
- `sitemap.ts`, `robots.ts`
- `/notes`, `/principles`, `/stats`는 **`robots.txt`에서 `Disallow`** — 개인 기록은 검색 노출 제외
- favicon, apple-touch-icon
- 404 / error 페이지

### 11.4 개인정보 안전장치
- 콘텐츠·코드에 **실명, 계좌번호, 증권사 계정, 실제 자산 총액을 절대 넣지 않는다**
- `.env`, 개인 데이터 백업 JSON은 `.gitignore`에 등록
- 계산기에 입력한 자본 금액은 URL 쿼리스트링에 싣지 않는다 (공유 링크로 새어나감)

### 11.5 데이터 백업 (M2에 포함, 생략 불가)
- 설정 페이지에 **내보내기** 버튼 → 전체 `ProgressStore`를 JSON 파일로 다운로드
- **불러오기** 버튼 → JSON 업로드로 복원 (버전 체크 후 마이그레이션)
- 완료한 레슨이 10개 늘 때마다 "백업하세요" 배너 1회 노출
- 이유: localStorage는 브라우저 캐시를 지우면 사라진다. 8개월치 진도가 한 번에 날아갈 수 있다.

### 11.6 방문자 vs 본인 구분
로그인은 없다. 대신 `localStorage`에 데이터가 있으면 "본인 모드"로 간주한다.
- 데이터 없음 = 방문자: 레슨 전부 열람 가능(잠금 해제 UI 안내만 표시), 쓰기 UI 숨김
- 데이터 있음 = 본인: 진도·퀴즈·자기요약 작성 가능
- 즉 **방문자는 커리큘럼을 자유롭게 읽을 수 있고, 기록은 남길 수 없다.** 이게 가장 단순하고 충분한 구분이다.

---

## 12. 데이터 스키마 (v1 — 매매일지 제외)

```ts
type ProgressStore = {
  version: 1;
  lessons: Record<string, {
    status: 'locked' | 'available' | 'in_progress' | 'completed' | 'needs_review';
    quizScore: number | null;
    attempts: number;
    practiceDone: boolean;
    mySummary: string;          // 자기 요약. 3줄 미만이면 완료 불가
    completedAt: string | null;
    note: string;
  }>;
  srs: Record<string, { interval: number; dueAt: string; lapses: number }>;
  dailyTests: Record<string, {        // key: 'YYYY-MM-DD'
    score: number;                    // 맞힌 개수
    total: number;
    questionIds: string[];
    wrongIds: string[];
    completedAt: string;
  }>;
  exams: Record<string, {             // key: level (예: '0')
    attempts: { score: number; total: number; passed: boolean; at: string }[];
    passed: boolean;
  }>;
  questionStats: Record<string, { seen: number; wrong: number }>;  // 출제 가중치용
  streak: { current: number; longest: number; lastTestAt: string | null };
  settings: { theme: 'light' | 'dark' | 'system' };
};
```

> `journal` 필드는 v1에 없다. 추가할 때 `version: 2`로 올리고 `migrate1to2()`를 작성한다.
> 기존 데이터를 덮어쓰지 말 것.

---

## 13. 보류 항목 (지금 만들지 않는다)

지금 안 만드는 이유는 "필요 없어서"가 아니라 **아직 쓸 데이터가 없어서**다.
빈 폼을 미리 만들어두면 요구사항을 모르는 채로 설계하게 되고, 나중에 다 뜯어고쳐야 한다.

| 기능 | 만들 시점 | 신호 |
|---|---|---|
| **매매일지** | Level 6 완료 후 | 모의투자로 실제 거래를 시작할 때 |
| 투자원칙서(IPS) | Level 9 진입 시 | 실전 소액 계좌를 열 때 |
| 성과 통계 (샤프·MDD·Profit Factor) | 일지 30건 이상 쌓인 뒤 | 계산할 데이터가 생겼을 때 |
| DB 이전 (SQLite + Prisma) | 기기를 2대 이상 쓰게 될 때 | localStorage로 불편해질 때 |
| 실시간 시세 연동 | 필요해지면 | 지금은 학습 방해 요소다 |

### 매매일지 사양 (나중에 그대로 꺼내 쓸 것)
- 필드: 날짜 / 종목 / 방향 / 진입가 / 청산가 / 수량 / 손익 / 적용 전략 / **규칙 준수 O·X** / 진입 시 감정(1~10) / 청산 시 감정 / 배운 점
- 핵심 화면: **규칙 준수 거래 vs 위반 거래의 손익 비교.** 이 그래프 하나가 이 기능의 존재 이유다.
- 공개 사이트이므로 항목별 `visibility` 필드 필요, 기본값 `private`, 금액은 `percent` 모드 기본.
